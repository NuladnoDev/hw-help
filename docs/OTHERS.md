# Команды

> 💡 **Важно:** Все команды бота можно использовать как с префиксом `!`, так и без него (например, `!инфа` или просто `инфа`).

### 1. Инфа
Выдает случайную вероятность для указанного события.
- **Команда:** `!инфа <текст>` или `инфа <текст>`
- **Пример:** `!инфа сегодня пойдет снег`
- **Ответ:** `📊 Вероятность составляет: 42%`

### 2. Данет
Помогает принять решение, выбирая между «Да» и «Нет».
- **Команда:** `!данет` или `данет`
- **Ответ:** `✅ Да` или `❌ Нет`

### 3. Кто
Выбирает случайного участника чата для выполнения какого-либо действия.
- **Команда:** `!кто <текст>` или `кто <текст>`
- **Пример:** `!кто самый крутой`
- **Ответ:** `🔎 Я думаю, что самый крутой — это @username`

### 4. Выбери
Выбирает один вариант из нескольких предложенных.
- **Команда:** `!выбери <вариант1> <вариант2> ...`
- **Пример:** `!выбери чай кофе сок`
- **Ответ:** `🤔 Я выбираю: кофе`
- *Примечание: Варианты можно разделять пробелами или запятыми.*

### 5. Пинг
Проверяет работоспособность бота и показывает задержку сети.
- **Команда:** `!пинг` или `пинг`
- **Ответ:** 
  ```
  ✅ На месте!
  ⏱ Пинг: 45 мс
  ```

## Управление
Каждую команду можно включить или выключить отдельно в настройках управления модулями чата:
- `Инфа` (ID: `info`)
- `Данет` (ID: `yesno`)
- `Кто` (ID: `who`)
- `Выбери` (ID: `choose`)
- `Пинг` (ID: `ping`)
